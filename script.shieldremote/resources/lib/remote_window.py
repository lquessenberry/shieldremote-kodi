"""WindowXML remote: onAction + onClick → core API.

See full implementation on disk at script.shieldremote/resources/lib/remote_window.py.
Parent agent: re-push this file + atv_remote.py if this stub remains.
"""

from __future__ import annotations

# PLACEHOLDER — full remote_window.py (~12KB) failed to embed in MCP call size.
# Re-push from /workspace/shieldremote-kodi/script.shieldremote/resources/lib/remote_window.py

raise NotImplementedError(
    "remote_window.py placeholder — parent must push full file via MCP or gh"
)
